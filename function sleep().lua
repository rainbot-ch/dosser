chance = 
basebet = 
bethigh = false
nextbet = basebet

---------------------------------this is the sleep function--local clock = os.clock
local clock = os.clock
function sleep(n)
  local t0 = clock()
  while clock() - t0 <= n do end
end
---------------------------------this is the sleep function--

function dobet()
 
        sleep(2) --bet latency in seconds eg. 2 means sleeping 2 seconds
        
    if else blabla
-------------------------------------------------

reset_at = os.clock()
if (reset_at + 300) <= os.clock() then
    reset_at = os.clock()
    resetseed()
end
-------------------------------------------------
sleepTimer = ((bets % 1000) + 1000)
-- make sure you convert sleepTimer to secs.
sleep(sleepTimer)
-------------------------------------------------
delay = 0
local clock = os.clock
function sleep(n)
    local t0 = clock()
    while clock() - t0 <= n do
    end
end
function dobet()
    n = delay
    sleep(n)
    print(delay)
    if win then
        delay = 0
    end
    if (nextbet >= dangerousbetsize) then
        delay = 1
    end
local a=os.clock;function sleep(b)local c=a()while a()-c<=b do end end;
--------------------------------------      
function sleep(n)
    t0 = os.clock() 
    while os.clock() - t0 <= n do end
end      
--------------------------------------  
